import * as c from "../actions/action-constants";

const initialState = {
  studentList: []
}

export function studentReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
